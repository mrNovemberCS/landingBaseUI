export enum ProjectMainnet {
  None = "None",
  Testnet = "Testnet",
  Mainnet = "Mainnet",
}

export enum ProjectStatus {
  REGISTERED = "Registered",
  APPROVED = "Approved",
}
export enum FormChecboxStatus {
  WARNING = "WARNING",
  VERIFY = "VERIFY",
  PREMIUM = "PREMIUM",
}
export enum ProjectFilter {
  All = "All",
  NATIVE = "Native",
  CROSSCHAIN = "Crosschain",
}
